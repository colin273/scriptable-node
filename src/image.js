class Image {
  constructor(data) {
    // Add ArrayBuffer as data property
    // this.size = size of image, in pixels--read only
  }
}

module.exports = {
  fromFile: function(filePath) {
  },

  fromData: function(data) {
  }
}