class WebView {
  static async loadHTML(html, baseURL, preferredSize, fullscreen) {
  }

  static async loadFile(fileURL, preferredSize, fullscreen) {
  }

  static async loadURL(url, preferredSize, fullscreen) {
  }

  async loadURL(url) {
  }

  async loadRequest(request) {
  }

  async loadHTML(html, baseURL) {
  }

  async loadFile(fileURL) {
  }

  async evaluateJavaScript(javaScript, useCallback) {
  }

  async getHTML() {
  }

  async present(fullscreen) {
  }

  async waitForLoad() {
  }
}

module.exports = WebView;